version: '3.9'

services:

  db:
    image: postgres
    container_name: creator_registry_db
    restart: always
    shm_size: 128mb
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: contentcreator
    ports:
      - "5432:5432"

